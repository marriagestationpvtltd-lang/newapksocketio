# CORS is handled entirely by each PHP file via header() calls.
# Do NOT add Access-Control-Allow-Origin here — all api9 PHP files already
# set it, so mod_headers would produce a duplicate "*, *" header that
# browsers reject as an invalid CORS response.
